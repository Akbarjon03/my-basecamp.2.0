<br/>
  <div class="signupdiv"><br/> <br/>
    <h3>Log in</h3>
  <div class="all">
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <%= f.label :email, class: "namelogin" %><br />
    <%= f.email_field :email, class: "emailinput", placeholder:"Email address", autofocus: true, autocomplete: "email" %>

    <%= f.label :password, class: "passname" %><br />
    <%= f.password_field :password, class: "passwordinput", placeholder:"Password",  autocomplete: "current-password" %>


    <% if devise_mapping.rememberable? %>
      <div class="field"> <br />
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "Log in", class:"signinbutton" %>
    </div>
    <% end %>
  
    <%= render "devise/shared/links" %>
  </div>
</div>